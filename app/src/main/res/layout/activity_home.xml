<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="#F5F5F5"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeActivity">

    <!-- Dashboard Summary Section -->
    <LinearLayout
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@android:color/white"
        android:elevation="4dp"
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="Today’s Summary"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            android:textColor="@android:color/black" />

        <LinearLayout
            android:orientation="horizontal"
            android:gravity="space_between"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Sleep summary -->
            <LinearLayout
                android:orientation="vertical"
                android:gravity="center"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content">
                <TextView
                    android:text="Sleep"
                    android:textStyle="bold"
                    android:textSize="16sp" />
                <TextView
                    android:text="7h 45m"
                    android:textColor="#4CAF50"
                    android:textSize="14sp" />
            </LinearLayout>

            <!-- Stress summary -->
            <LinearLayout
                android:orientation="vertical"
                android:gravity="center"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content">
                <TextView
                    android:text="Stress"
                    android:textStyle="bold"
                    android:textSize="16sp" />
                <TextView
                    android:text="Low"
                    android:textColor="#FF9800"
                    android:textSize="14sp" />
            </LinearLayout>

            <!-- Energy Score summary -->
            <LinearLayout
                android:orientation="vertical"
                android:gravity="center"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content">
                <TextView
                    android:text="Energy"
                    android:textStyle="bold"
                    android:textSize="16sp" />
                <TextView
                    android:text="82%"
                    android:textColor="#2196F3"
                    android:textSize="14sp" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Scrollable Main Feature Cards -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:orientation="vertical"
            android:padding="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Sleep Tracking -->
            <include layout="@layout/card_home_feature" android:id="@+id/cardSleep"
                android:tag="Sleep Tracking" />

            <!-- Stress Monitoring -->
            <include layout="@layout/card_home_feature" android:id="@+id/cardStress"
                android:tag="Stress Monitoring" />

            <!-- Energy Score -->
            <include layout="@layout/card_home_feature" android:id="@+id/cardEnergy"
                android:tag="Energy Score" />

            <!-- Fitness Programs -->
            <include layout="@layout/card_home_feature" android:id="@+id/cardFitness"
                android:tag="Fitness Programs" />

            <!-- Together Mode -->
            <include layout="@layout/card_home_feature" android:id="@+id/cardTogether"
                android:tag="Together Mode" />
        </LinearLayout>
    </ScrollView>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/nav_item_selector"
        app:itemTextColor="@color/nav_item_selector" />

</LinearLayout>
